#include "conversion.h"

unsigned char intToChar(int number)	// explicit typecast int to char
{
	return number;
}

int charToInt(unsigned char character)	// explicit typecast char to in
{
	return character;
}